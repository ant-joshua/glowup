# Checklist
- [ ] PRD-001..PRD-006 documents under `docs/product/` are reflected in an explicit UI route map (landing + subpages per PRD).
- [ ] App has a top-level navigation that routes to all six PRD areas.
- [ ] Each PRD area page renders using mock API data only (no external dependencies required).
- [ ] Mock API endpoints exist for all six PRD areas and return valid JSON.
- [ ] Key example endpoint(s) respond successfully in dev (e.g., via browser or curl).
- [ ] `apps/web` passes lint.
- [ ] `apps/web` production build succeeds.

