# Checklist
- [x] PRD-001..PRD-006 documents under `docs/product/` are reflected in an explicit UI route map (landing + subpages per PRD).
- [x] App has a top-level navigation that routes to all six PRD areas.
- [x] Each PRD area page renders using mock API data only (no external dependencies required).
- [x] Mock API endpoints exist for all six PRD areas and return valid JSON.
- [x] Key example endpoint(s) respond successfully in dev (e.g., via browser or curl).
- [x] `apps/web` passes lint.
- [x] `apps/web` production build succeeds.
